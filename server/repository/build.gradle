

bootJar {
    enabled = false
}

jar {
    enabled = true
}

dependencies {
    compile project(":entity_model")
    compile group: 'org.springframework.boot', name: 'spring-boot-starter-data-jpa'
    compile group: 'mysql', name: 'mysql-connector-java', version: '8.0.14'
}